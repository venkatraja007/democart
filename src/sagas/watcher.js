import { triggerLogin } from "./allSagas";
import * as types from "../constants/actionTypes";
//action type asynchronously
import { takeLatest } from "redux-saga/effects";
export default function* getAllRequests() {
  yield [];
}
